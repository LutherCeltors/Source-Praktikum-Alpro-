#include "listlinier.h"
#include <stdio.h>

int main(){
   List l;
   CreateList(&l);  
   ElType val, out;
   for (int i = 0; i < 3; i++){
       scanf("%d",&val);
       insertFirst(&l, val);
   }
   displayList(l);
   deleteLast(&l);
   displayList(l, &out);
   printf("%d",out);
   return 0;
}
